


## 🕐 1. Date 类


### 常用方法

| 方法                           | 作用          | 返回值类型     |
| ---------------------------- | ----------- | --------- |
| `Date.today()`               | 取得今天的日期     | `Date`    |
| `Date.new(year, month, day)` | 创建指定日期      | `Date`    |
| `addDays(n)`                 | 当前日期加上n天    | `Date`    |
| `addMonths(n)`               | 当前日期加上n个月   | `Date`    |
| `addYears(n)`                | 当前日期加上n年    | `Date`    |
| `daysBetween(date2)`         | 计算两个日期之间的天数 | `Integer` |
| `isLeapYear(year)`           | 判断闰年        | `Boolean` |

### 🌰 示例

```apex
Date today = Date.today(); // 今天的日期
Date birthday = Date.new(1995, 5, 10); // 1995年5月10日
Date nextWeek = today.addDays(7); // 一周后
Integer diff = today.daysBetween(birthday); // 相差的天数
Boolean leap = Date.isLeapYear(2024); // 是否闰年？
System.debug('今天：' + today + ' 一周后：' + nextWeek);
System.debug('距离生日天数：' + diff);
System.debug('2024是闰年吗？' + leap);
```

---

## ⏰ 2. Datetime 类


### 常用方法

| 方法                                                       | 作用      | 返回值        |
| -------------------------------------------------------- | ------- | ---------- |
| `Datetime.now()`                                         | 当前日期和时间 | `Datetime` |
| `Datetime.newInstance(year, month, day, hour, min, sec)` | 创建指定时间  | `Datetime` |
| `addDays(n)` / `addHours(n)` / `addMinutes(n)`           | 时间加减    | `Datetime` |
| `format()`                                               | 按区域格式化  | `String`   |
| `date()`                                                 | 取出日期部分  | `Date`     |
| `time()`                                                 | 取出时间部分  | `Time`     |

### 🌰 示例

```apex
Datetime now = Datetime.now();
Datetime meeting = Datetime.newInstance(2025, 12, 1, 14, 30, 0);
Datetime later = now.addHours(3);
System.debug('现在：' + now.format());
System.debug('会议时间：' + meeting);
System.debug('三小时后：' + later);
```

---

## 💰 3. Decimal 类


### 常用方法

| 方法                                                 | 作用          | 返回值       |
| -------------------------------------------------- | ----------- | --------- |
| `add()` / `subtract()` / `multiply()` / `divide()` | 数学运算        | `Decimal` |
| `round(scale)`                                     | 四舍五入到几位     | `Decimal` |
| `setScale(scale, mode)`                            | 设置小数位及舍入方式  | `Decimal` |
| `valueOf(string)`                                  | 字符串转Decimal | `Decimal` |

### 🌰 示例

```apex
Decimal price = 99.99;
Decimal tax = 0.08;
Decimal total = price.multiply(1 + tax);
System.debug('含税价：' + total.round(2));
```

---

## 🧮 4. Double 类


### 🌰 示例

```apex
Double pi = 3.1415926;
Double radius = 10.5;
Double area = Math.PI * Math.pow(radius, 2);
System.debug('圆面积：' + area);
```

---

## 🆔 5. Id 类


### 常用方法

| 方法                       | 作用         |
| ------------------------ | ---------- |
| `Id.valueOf(String)`     | 将字符串转成Id对象 |
| `getSObjectType()`       | 返回对象类型     |
| `isSalesforceId(string)` | 判断是否为有效Id  |

### 🌰 示例

```apex
Id userId = UserInfo.getUserId();
System.debug('当前用户Id：' + userId);
```

---

## 🔢 6. Integer 类


### 🌰 示例

```apex
Integer a = 10;
Integer b = 3;
Integer sum = a + b;
System.debug('和：' + sum);
```

---

## 🔤 7. String 类


### 常用方法

| 方法                                | 作用    |
| --------------------------------- | ----- |
| `length()`                        | 字符数   |
| `toLowerCase()` / `toUpperCase()` | 大小写转换 |
| `substring(start, end)`           | 截取子串  |
| `split(separator)`                | 分割    |
| `contains()` / `startsWith()`     | 判断包含  |
| `valueOf(obj)`                    | 转字符串  |

### 🌰 示例

```apex
String name = 'Salesforce';
System.debug(name.substring(0, 5)); // Sales
System.debug(name.toUpperCase());   // SALESFORCE
System.debug(name.contains('force')); // true
```

---

## 📐 8. Math 类


### 常用方法

| 方法                                  | 说明     |
| ----------------------------------- | ------ |
| `Math.abs()`                        | 绝对值    |
| `Math.floor()`                      | 向下取整   |
| `Math.ceil()`                       | 向上取整   |
| `Math.round()`                      | 四舍五入   |
| `Math.pow(a, b)`                    | a^b    |
| `Math.sqrt()`                       | 平方根    |
| `Math.max(a, b)` / `Math.min(a, b)` | 最大最小值  |
| `Math.random()`                     | 0~1随机数 |

### 🌰 示例

```apex
Decimal n = -5.7;
System.debug(Math.abs(n)); // 5.7
System.debug(Math.floor(n)); // -6
System.debug(Math.random()); // 随机小数
```

---

## 🧺 9. List 类


### 常用方法

| 方法              | 作用     |
| --------------- | ------ |
| `add(value)`    | 加入元素   |
| `get(index)`    | 取指定位置  |
| `size()`        | 元素数    |
| `isEmpty()`     | 是否为空   |
| `remove(index)` | 删除指定位置 |
| `clear()`       | 清空     |
| `sort()`        | 排序     |

### 🌰 示例

```apex
List<String> fruits = new List<String>{'apple', 'banana'};
fruits.add('cherry');
System.debug(fruits.size()); // 3
System.debug(fruits.get(1)); // banana
```

---

## 🧳 10. Set 类


### 🌰 示例

```apex
Set<String> colors = new Set<String>{'red', 'blue', 'red'};
System.debug(colors); // 只有 red, blue
System.debug(colors.contains('blue')); // true
```

---

## 🗺️ 11. Map 类


### 常用方法

| 方法                 | 作用        |
| ------------------ | --------- |
| `put(key, value)`  | 添加键值对     |
| `get(key)`         | 取值        |
| `containsKey(key)` | 是否有此键     |
| `remove(key)`      | 删除键值对     |
| `keySet()`         | 所有key集合   |
| `values()`         | 所有value集合 |

### 🌰 示例

```apex
Map<String, Integer> scores = new Map<String, Integer>();
scores.put('Tom', 90);
scores.put('Amy', 85);
System.debug(scores.get('Tom')); // 90
System.debug(scores.keySet());   // 所有key
```

---

## 📩 12. Messaging 类


### 常用方法

| 方法                             | 用途     |
| ------------------------------ | ------ |
| `Messaging.SingleEmailMessage` | 发送单封邮件 |
| `Messaging.sendEmail()`        | 发送邮件动作 |

### 🌰 示例

```apex
Messaging.SingleEmailMessage mail = new Messaging.SingleEmailMessage();
mail.setToAddresses(new String[] {'test@example.com'});
mail.setSubject('Hello');
mail.setPlainTextBody('This is a test.');
Messaging.sendEmail(new Messaging.SingleEmailMessage[] { mail });
```

---

## 🌐 13. Url 类


### 常用方法

| 方法                       | 作用      |
| ------------------------ | ------- |
| `getSalesforceBaseUrl()` | 当前组织域名  |
| `getOrgDomainUrl()`      | 组织URL   |
| `getCurrentRequestUrl()` | 当前请求URL |

### 🌰 示例

```apex
System.debug(URL.getSalesforceBaseUrl().toExternalForm());
```

---

## 👤 14. UserInfo 类


### 常用方法

| 方法                    | 作用           |
| --------------------- | ------------ |
| `getUserId()`         | 当前用户Id       |
| `getUserName()`       | 用户名          |
| `getProfileId()`      | 用户Profile Id |
| `getLocale()`         | 地区           |
| `getTimeZone()`       | 时区           |
| `getOrganizationId()` | 组织Id         |

### 🌰 示例

```apex
System.debug('用户ID：' + UserInfo.getUserId());
System.debug('组织ID：' + UserInfo.getOrganizationId());
System.debug('用户名：' + UserInfo.getUserName());
```